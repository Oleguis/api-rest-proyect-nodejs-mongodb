# Elaborando una API REST desde cero con la guia de un tutorial en BluuWeb

    utilizando

        - nodeJs.
        - express.
        - express-validation.
        - mongoose (mongodb).
        - jsonWebToken (jwt).